generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

//User with is as int autoincrement, name as string and email as string

model User {
  id    Int   @id @default(autoincrement())
  name  String
  email String
}